# Full local / production-style install (API + crawlers + ML).
# Default GitHub CI uses requirements-ci.txt only (see .github/workflows/ci.yml).

-r requirements-ci.txt
-r requirements-ml.txt
torch>=2.6.0
