{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": ["npm ci", "npm run build"]
    }
  },
  "deploy": {
    "startCommand": "npx --yes serve@14.2.4 -s dist -l 8080"
  }
}
