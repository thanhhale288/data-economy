name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      ml: ${{ steps.filter.outputs.ml }}
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'crawlers/**'
              - 'pipeline/**'
              - 'ml/**'
              - 'tests/**'
              - 'data/seeds/**'
              - 'requirements*.txt'
              - 'pytest.ini'
              - '.github/workflows/ci.yml'
              - 'alembic/**'
              - 'scripts/**'
            ml:
              - 'ml/**'
              - 'tests/ml/**'
              - 'tests/e2e/**'
              - 'tests/shop_matcher/**'
              - 'requirements-ml.txt'
              - 'requirements.txt'
              - 'backend/app/api/ml.py'
              - 'backend/app/api/ml_monitoring.py'
              - 'backend/app/services/ml_*.py'
              - 'backend/app/services/forecast_narrative.py'
              - 'backend/app/services/anomaly_service.py'
              - '.github/workflows/ci.yml'
            frontend:
              - 'frontend/**'

  backend:
    name: Backend tests (lean)
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Install lean dependencies
        run: pip install -r requirements-ci.txt

      - name: Run pytest (exclude ml / e2e)
        env:
          PYTHONPATH: .
        run: pytest -q -m "not ml"

  backend-ml:
    name: Backend ML tests
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.ml == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements-ci.txt
            requirements-ml.txt

      - name: Install lean + ML dependencies (torch CPU)
        run: |
          pip install -r requirements-ci.txt
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements-ml.txt

      - name: Run pytest (ml marker only)
        env:
          PYTHONPATH: .
          OMP_NUM_THREADS: "1"
          OPENBLAS_NUM_THREADS: "1"
          SHOP_MATCHER_BACKEND: tfidf
        run: pytest -q -m ml

  frontend:
    name: Frontend build
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

  # Required check when path filters skip every job (e.g. docs-only PR).
  ci-gate:
    name: CI gate
    if: always()
    needs: [changes, backend, backend-ml, frontend]
    runs-on: ubuntu-latest
    steps:
      - name: Fail if any executed job failed
        run: |
          results='${{ join(needs.*.result, ',') }}'
          echo "needs results: $results"
          if echo "$results" | grep -q failure; then
            exit 1
          fi
          if echo "$results" | grep -q cancelled; then
            exit 1
          fi
