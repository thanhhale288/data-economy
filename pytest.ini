[pytest]
markers =
    ocr: requires optional PaddleOCR extra (requirements-ocr.txt)
    ml: requires requirements-ml.txt (+ torch); skipped on lean CI backend job
    e2e: offline pipeline chain; also requires ml extras
