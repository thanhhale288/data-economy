# Extraction cascade raw inputs (Evol-1 T05)

- `cohort.json` — written by the runner (listed28 ± frame URLs).
- `frame_urls.json` — optional list of `{firm_id, website_url, ...}` from URL-finder
  on the T02 pilot frame. Without this file, the cascade runs **listed28 only**.

Do not store marketplace listing scrapes here. Indicators come from company websites.
