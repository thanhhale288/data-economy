# Conflict notes (tier1 rules vs tier2 LLM)

Interesting mismatches for advisor slides — not a gold-standard evaluation.

- No bool/presence conflicts in this run (common when tier2 is disabled/abstain).
